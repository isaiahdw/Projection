# projection_new

`projection_new` is a Mix archive that generates a starter Projection + Slint app.

## Build archive locally

```bash
cd projection_new
mix archive.build
mix archive.install
```

## Usage

```bash
mix projection.new my_app
mix projection.new my_app --module MyApp --app my_app
```
